# HuggingFace Hub token — enables higher rate limits and access to gated models.
# Get one at: https://huggingface.co/settings/tokens
# HF_TOKEN=hf_...

# MLFlow — silence the agent hint popup
MLFLOW_DISABLE_AGENT_HINT=1
